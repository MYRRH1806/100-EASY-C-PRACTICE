#include <iostream>
#include <string>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int a;
    cout<< "Enter number to be checked: "<<flush;
    cin>>a;
    string s = (a%2==0) ? "even" : "odd";
    cout<< "Number is "<< s;
    
    
}
