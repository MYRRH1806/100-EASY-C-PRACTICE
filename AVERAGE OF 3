#include <iostream>
#include <string>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    int a,b,c;
    cout<< "Enter three numbers: "<<flush;
    cin>>a>>b>>c;
    int avg = (a+b+c)/3;
    cout<< "Average of numbers is "<< avg;
    
    
}
